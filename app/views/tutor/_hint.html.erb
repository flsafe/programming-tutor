<%= link_to_function "Show Hint #{hint_counter + 1}", :id=>"hint#{hint_counter}" do |page| 
		page.insert_html :bottom, "hints", "#{ hint.text }"
		page.remove "hint#{hint_counter}"
end %>&nbsp;&nbsp;&nbsp;