page.replace_html :grade, h(@grade_sheet.unit_test_results) if @grade_sheet
page.replace_html :message, @message if @message
if @message == "grading..."
  page << "keepPollingGrade = true"
else
  page << "keepPollingGrade = false"
end