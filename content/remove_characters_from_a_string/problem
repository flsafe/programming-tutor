<p>
Write a function <i>remove_from_str</i> that takes two arguments. The first argument is a 
character array <i>remove_chars</i>. The second argument is a 
character array <i>str</i>. The function removes the characters in <i>remove_chars</i>
from <i>str</i>.
</p>

<p> For example: </p>

<pre><code>
void remove_from_str(char remove_chars[], char str[]);

remove_from_str("c", "acacac") => "aaa"

remove_from_str("c", "aaaac")  => "aaaa"
	
remov_from_str("c", "abab")    => "abab"
</code></pre>

<p>
Notice that the function remove_from_str returns
void and modifies the character array str in place.
Assume ASCII characters.
</p>
